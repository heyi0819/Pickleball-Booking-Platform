# S10.4 Real LINE / LIFF Acceptance

S10.4 enables the existing staging LIFF frontend to use a separately owned, non-production LINE Login channel. The maintainer-approved progressive-profile repair removes phone collection and storage, while preserving the approved LINE credential verification and Platform JWT architecture. It adds no paid resource, production resource, Messaging API channel, webhook, or worker.

## Scope and boundary

| Component | Non-sensitive identifier | Boundary |
| --- | --- | --- |
| LINE provider | `pickleball-stg` | Separate staging-only provider |
| LINE Login channel | `pickleball-stg` (`2011349413`) | Published; no Messaging API resource |
| LIFF app | `pickleball-stg-liff` (`2011349413-coxfchhN`) | Full-size endpoint at `https://pickleball-stg-liff.pages.dev` |
| Render API | `pickleball-stg-api` | Free staging service |
| Cloudflare Pages LIFF | `pickleball-stg-liff.pages.dev` | Free staging Pages project |

The provider and channel are deliberately separate from the legacy LINE resources. The LIFF app has only the `openid` and `profile` scopes, has `chat_message.write` disabled, and has add-friend disabled.

## Configuration and deployment evidence

The staging configuration contains only the public LINE Login channel identifier:

```text
LINE_LOGIN_CHANNEL_ID=2011349413
VITE_LIFF_ID=2011349413-coxfchhN
```

`VITE_LIFF_ID` was added as a public Cloudflare build variable and a new Pages deployment succeeded from main commit `e6eaea092d2c45243dfacfb927e7d3ca6c07097c` (deployment `490a16ec-9e0b-4ca4-87a2-436443a96c88`).

`LINE_LOGIN_CHANNEL_ID` was added to the staging Render service.  After the controlled schema migration, deployment `dep-dab3vdfavr4c73enktqg` became Live from PR #42 commit `55a25ac0259b1286b5adc68ff13944a3d63ca571`, which is compatible with V13. Render started the Java 21 application on its injected port and retained the existing staging settings:

```text
SPRING_FLYWAY_ENABLED=false
WORKERS_ENABLED=false
APP_CORS_ALLOWED_ORIGINS=https://pickleball-stg-admin.pages.dev,https://pickleball-stg-liff.pages.dev
```

Cloudflare Pages also built a successful PR preview for commit `55a25ac` at `https://24750118.pickleball-stg-liff.pages.dev`. The configured LIFF endpoint remains the canonical staging Pages URL; the preview is evidence for the PR artifact only, not a replacement registered LIFF endpoint.

For the controlled S10.4 acceptance window, the staging-only Pages project's Production branch is `feat/s10.4-real-line-liff-acceptance`. This deliberately serves the PR frontend at the registered canonical staging LIFF URL without merging the PR or changing any production resource.

No Channel Secret, access token, ID token, platform JWT, database credential, or personal identity is recorded here or in Git.

## External smoke and security evidence

All requests below were made after the Render deployment became Live.

| Check | Result |
| --- | --- |
| `GET /actuator/health` | `200` / `UP` |
| `POST /api/v1/auth/line/login` with an intentionally invalid token | `401` |
| `GET /api/v1/me` with an intentionally invalid platform JWT | `401` |
| LIFF Pages origin CORS preflight | `200`, exact LIFF origin, no credentialed CORS |
| Admin Pages origin CORS preflight | `200`, exact Admin origin, no credentialed CORS |
| Unknown-origin CORS preflight | `403` |

Loading `https://pickleball-stg-liff.pages.dev` reaches the separate staging LINE Login authorization flow for channel `2011349413`. After maintainer-controlled LINE consent, the mobile client returned to the LIFF endpoint and displayed the authenticated STUDENT entry. This demonstrates the accepted chain from LIFF launch through backend verification, platform identity mapping, Platform JWT issuance, and the authenticated current-user page; no credential or personal identifier was retained as evidence.

## Progressive profile and data minimization

The original real-device acceptance exposed an onboarding defect: `profileComplete` treated phone or email as required and the LIFF client redirected a successful LINE login to a mandatory profile form. The repair applies the following product decision:

- Verified LINE subject remains the only trusted external identity; display name and email are not authentication identifiers.
- Initial provisioning uses the verified LINE display name (with the existing safe fallback), locale, active status, and default role assignment.
- `profileComplete` remains in the API for compatibility, but now means platform-required data is present. A provisioned user is complete without email.
- Email remains nullable and can still be updated through `PATCH /api/v1/me/profile`.
- Phone is removed from the user entity, API request/response, OpenAPI source, generated TypeScript client, LIFF UI, tests, and staging schema.

### Forward-only schema change

`V13__remove_user_phone.sql` drops `idx_users_phone` and then `users.phone`. The final `users` shape retains `id`, non-null `display_name`, nullable `email`, status, locale, timestamps, version, and `deleted_at`; its email and status indexes remain. There is no down migration: rollback uses a reviewed forward fix.

The staging boundary is synthetic-only. A value-free lineage verification for the one legacy phone-bearing row confirmed the `pickleball-stg` resource, the staging default organization and active role, a staging-acceptance creation time, a LINE external identity, and zero payment, receivable, settlement, payout, course, enrollment, or other production-like business linkages. The maintainer therefore approved the synthetic-data conclusion without exposing any personal value.

On 2026-09-01, the maintainer ran the repository-defined controlled Flyway migration container with `SPRING_PROFILES_ACTIVE=migration` and the existing `pickleball_migrator` role. Flyway validated 13 migrations, applied V13 successfully, and exited normally. Value-free post-migration verification confirmed all of the following: V13 is successful; `users.phone` and `idx_users_phone` are absent; `email` is nullable; `display_name` remains non-null; existing user rows remain; `users` remains owned by `pickleball_migrator`; and `pickleball_app` is not a member of that migrator role.

## Automated verification

Executed on this branch after the progressive-profile repair:

| Command | Result |
| --- | --- |
| `backend/mvnw.cmd test` | PASS — 174 tests, 0 failures and 0 errors; PostgreSQL 18 Testcontainers and clean Flyway V1–V13 exercised |
| `frontend/npm run typecheck` | PASS |
| `frontend/npm run lint` | PASS |
| `frontend/npm test` | PASS — 13 tests |
| `frontend/npm run build` | PASS |
| `frontend/npm run api:check` | PASS |
| `frontend/npx playwright test --workers=1` | PASS — 14 tests; the default 4-worker run timed out locally during concurrent Vite startup, while the serial rerun completed without a test failure |

The existing LINE verifier tests validate issuer, audience, expiration, malformed-token rejection, and the server-owned mapping from a verified LINE identity to the platform session. Identity security tests now prove a first LINE user with no email receives a complete usable `/api/v1/me` response without a `phone` property, permits an optional valid email update, rejects an invalid email, and retains invalid-token and CORS boundaries. The LIFF unit and Slice 1 Playwright tests prove that null email does not render the mandatory profile/phone form and proceeds to role selection.

## Real-device acceptance

The maintainer accepted the staging LINE consent flow and launched the LIFF app in a mobile LINE/browser context. The representative result was a successful LIFF launch at the canonical Pages URL, with authenticated STUDENT entry and the existing member/profile page rendered. The accompanying mobile evidence is intentionally not committed because it can contain personal UI context.

For a repeat acceptance, launch:

```text
https://liff.line.me/2011349413-coxfchhN
```

Then verify that the LIFF app returns to `pickleball-stg-liff.pages.dev`, exchanges the LINE ID token successfully, and loads the authenticated current-user state. Report only success/failure and any non-sensitive error text; do not send an ID token, platform JWT, LINE user ID, password, MFA value, Channel Secret, or access token.

After V13 and the V13-compatible API deployment, the maintainer repeated this acceptance at 2026-09-01 12:12 Asia/Taipei with a fresh LIFF launch. It passed: the client entered `STUDENT entry` directly, rendered authenticated member content, and showed no mandatory name, phone, or email onboarding gate. The visual evidence is intentionally not committed because it includes member UI context.

## Deferred frontend polish

The real-device screen confirms the architecture and authentication path, but the current sealed frontend still includes English labels and uses a basic mobile layout. Full Traditional Chinese localization and responsive visual refinement are a follow-up frontend scope; they are intentionally not mixed into this auth acceptance slice.

## Rollback and forward-fix

- To disable this staging login path, first remove `VITE_LIFF_ID` from the Pages build configuration and redeploy, then remove `LINE_LOGIN_CHANNEL_ID` from Render and redeploy.
- To revoke compromised LINE credentials, use the owning LINE Developers Console and update the single staging consumer; do not place replacement values in Git.
- For the privacy repair, apply V13 only after the value-free staging count confirms the approved synthetic-data boundary. If a forward fix is required after migration, add a new migration; never alter V1–V13.
- Do not alter the published staging channel's scopes, add a Messaging API resource, enable a webhook, or alter production resources as part of this slice.
